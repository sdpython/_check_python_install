dist: xenial
sudo: true
language: python
python:
    - "3.7"
addons:
-  apt:
-    packages:
      - libgeos-dev
      - libproj-dev
      - proj-bin
      - proj-data
install: 
    - pip install -r requirements.txt

script:
    - python ./setup.py unittests
