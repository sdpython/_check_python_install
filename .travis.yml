dist: xenial
sudo: true
language: python
python:
    - "3.7"
addons:
-  apt:
-    packages:
      - libgeos-dev
install: 
    - pip install -r requirements.txt

script:
    - python ./setup.py unittests
